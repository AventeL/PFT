/// Exercise muscle group classification
/// For display names, use: AppStringsHelper.getMuscleGroupName(context, muscleGroup.name)
enum MuscleGroup { chest, back, shoulders, arms, legs, core }

/// Exercise category - compound vs isolation
/// For display names, use: AppStringsHelper.getCategoryName(context, category.name)
enum ExerciseCategory { compound, isolation }

/// Equipment type used for exercise
/// For display names, use: AppStringsHelper.getEquipmentName(context, equipment.name)
enum EquipmentType { barbell, dumbbell, machine, bodyweight, cable }
